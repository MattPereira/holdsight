# Restrict instances to approved users

Holdsight deployments will be closed instances: operators configure an explicit, default-deny email allowlist that may contain multiple trusted users, with each user's financial data remaining isolated. This supports small trusted groups without exposing the deployed application to public enrollment.
